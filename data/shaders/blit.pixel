#version 330 core

in vec2 UV;

out vec4 Colour;

uniform sampler2D Texture;
uniform bool DrawDepth;

void main()
{
	if( DrawDepth )
	{
		float depth = texture( Texture, UV ).r;
		float power = pow(1.0 - depth, 2);
    	Colour = vec4(power);
	}
	else
	{
		Colour = texture( Texture, UV );
	}
}